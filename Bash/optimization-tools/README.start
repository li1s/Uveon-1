Для запуска инструментов оптимизации используйте файл ./start
Файл config содержит основные настройки системы
В папке files находятся конфигурационные файлы для выполнения задач с установкой пакетов (pkg_required.txt), удалением пакетов (pkg_remove.txt) и остановкой служб (services.txt). Комментарии в данных файлах не поддерживаются. Файл читается построчно, поэтому, для удаления или добавления просто впишите новое значение в строку.
В файле fly-optimization.conf указаны параметры для оптимизации рабочего стола fly. Закомментируйте или раскомментируйте значение для его обработки.
После использования инструментов протестируйте гостевую ОС путем ее перезагрузки.

Вывод версии инструментов подготовки и оптимизации:
./start --version
